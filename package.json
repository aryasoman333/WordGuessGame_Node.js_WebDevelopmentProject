{
  "name": "exam01",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'exam1' (`git checkout -b exam1`)\r * modify the `questions.md` file to have the answers required\r * Add a `server.js`, a `words.js` and any other files necessary to create the multiple page web application below\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the TA and I as reviewers.  \r * Due by 11:59pm Sun Feb 10",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
